{
    "name": "stavarengo/php-dominoes",
    "type": "project",
    "config": {
        "sort-packages": true
    },
    "authors": [
        {
            "name": "Stavarengo",
            "email": "faelsta@gmail.com"
        }
    ],
    "autoload": {
        "psr-4": {
            "Dominoes\\": "src/"
        }
    },
    "require": {
        "php": "^8.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "scripts": {
        "test": "phpunit --colors=always",
        "test-coverage": "XDEBUG_MODE=coverage phpunit --colors=always --coverage-html ./test-coverage"
    }
}
